/**
 * Ventilation (Lüftung) — ventilation-only UI lives here.
 *
 * Shared: ../common, ../viewer, ../legend, ../floors
 * Pair with `dataViewMode === "luftung"`.
 */
export { default as VentilationZonePanel } from "./VentilationZonePanel";
